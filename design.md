# 社交图增强的GRU自回归传播预测模型设计方案

## 1. 项目概述

本设计旨在构建一个准确度高的序列到序列传播模型，实现以下目标：
- 给定一个传播序列，生成长度为3的后续传播序列
- 利用社交网络结构（Friendship边集合）增强预测效果
- 使用自回归方式预测，通过3次连续预测生成完整序列
- 采用调度采样与曝光偏差训练相结合解决自回归预测偏差问题

## 2. 数据处理与社交图构建

### 2.1 数据集概述
- **传播序列数据**：包含用户ID和时间戳的传播路径
- **社交网络数据**：用户间的Friendship关系（二元关系）

### 2.2 加权社交图构建
基于历史传播序列为社交网络边赋予权重，使用以下方法：

#### 融合权重计算
将三种权重方法融合，创建综合边权重：
1. **共现频率权重**
   ```
   W_freq(u,v) = count(u→v在序列中相邻出现) / total_cascades
   ```

2. **时间感知权重**
   ```
   W_time(u,v) = Σ exp(-λ * (t_v - t_u)) for all cascades where v follows u
   ```
   λ为时间衰减因子(0.1)

3. **传播效率权重**
   ```
   W_eff(u,v) = Σ 1 / (t_v - t_u + ε) for all cascades where v follows u
   ```
   ε为平滑因子(1.0)

4. **综合权重**
   ```
   W(u,v) = α * W_freq(u,v) + β * W_time(u,v) + γ * W_eff(u,v)
   ```
   α=0.4, β=0.3, γ=0.3（可通过验证集调优）

## 3. 模型架构

### 3.1 整体架构
```
传播序列 → 序列编码器(GRU) → 
                            ↘
                             融合层 → 自回归解码器(GRU) → 预测节点
                            ↗
加权社交图 → 图编码器(GAT) →
```

### 3.2 核心组件设计

#### 3.2.1 序列编码器
- **架构**：双向GRU网络
- **输入层**：
  - 节点嵌入：将节点ID映射到d_model维向量空间
  - 时间嵌入：将时间间隔映射为向量表示
- **处理流程**：双向GRU处理序列，捕捉传播模式
- **输出**：序列表示H_seq

#### 3.2.2 图编码器
- **架构**：图注意力网络(GAT)
- **输入**：节点嵌入和加权社交图
- **层数**：2-3层GAT
- **注意力**：多头注意力(8头)，利用边权重调整消息传递
- **输出**：节点的图结构表示H_graph

#### 3.2.3 融合层
- **机制**：门控融合
  ```
  gate = sigmoid(W_g * [H_seq; H_graph])
  H_fused = gate * H_seq + (1-gate) * H_graph
  ```
- **输出**：融合后的节点表示H_fused

#### 3.2.4 自回归GRU解码器
- **架构**：单向GRU网络
- **状态初始化**：使用编码器最终状态
- **输入**：当前序列的节点表示
- **预测过程**：逐步预测3个节点，每次将预测节点添加到输入序列
- **输出层**：全连接层+softmax，生成节点概率分布

## 4. 自回归预测偏差解决方案

### 4.1 调度采样策略
在训练过程中动态调整使用真实标签与模型预测的比例：

- **非线性衰减函数**：
  ```
  teacher_forcing_ratio = max(0.1, exp(-2.0 * epoch / total_epochs))
  ```

- **位置差异化采样率**：
  - 第一个位置：基础教师强制率
  - 第二个位置：基础率 * 0.9
  - 第三个位置：基础率 * 0.8

### 4.2 曝光偏差训练
显式训练模型从错误中恢复的能力：

- **混合输入序列**：按一定概率替换真实标签为模型预测或噪声
- **噪声注入策略**：
  ```
  noise_ratio = min(0.15, 0.05 + 0.1 * epoch / total_epochs)
  ```
- **错误恢复训练**：
  1. 构建含错误的输入序列
  2. 训练模型在此基础上正确预测后续节点

### 4.3 社交网络修正机制
使用社交图信息修正模型预测：

- **预测修正公式**：
  ```
  p_corrected = (1-α_t) * p_model + α_t * p_graph
  ```
  p_graph基于社交图计算，α_t随位置t增加(0.2→0.3→0.4)

- **修正动态调整**：
  - 训练早期：较小修正权重
  - 训练后期：逐渐增加修正权重

### 4.4 训练阶段划分
将训练过程分为三个阶段：

1. **预热阶段** (10% epochs)
   - 高教师强制率(0.9-0.8)
   - 低噪声注入率(0-5%)
   - 主要学习基本序列模式

2. **适应阶段** (40% epochs)
   - 教师强制率逐渐降低(0.8-0.4)
   - 噪声注入率增加(5-10%)
   - 开始适应自己的预测

3. **鲁棒性阶段** (50% epochs)
   - 低教师强制率(0.4-0.1)
   - 高噪声注入率(10-15%)
   - 重点训练从错误恢复的能力

## 5. 损失函数设计

### 5.1 位置加权预测损失
针对三个预测位置，使用不同权重的交叉熵损失：

```
L_pred = 0.5 * CE(p_1, y_1) + 0.3 * CE(p_2, y_2) + 0.2 * CE(p_3, y_3)
```

### 5.2 社交一致性损失
鼓励预测结果遵循社交网络结构：

```
L_social = -log(Σ W(prev_node, pred_node) / |prev_nodes|)
```

### 5.3 误差恢复损失
针对包含错误的输入序列，专门训练恢复能力：

```
L_recovery = CE(p_t | noisy_prefix, y_t)
```

### 5.4 总损失函数
```
L_total = L_pred + 0.2 * L_social + 0.3 * L_recovery
```

## 6. 训练过程

### 6.1 批次构建策略
- **多样化前缀**：从同一传播序列生成不同长度前缀
- **平衡样本**：确保不同长度和复杂度序列均衡分布
- **批次大小**：32-64，根据序列长度动态调整

### 6.2 优化器设置
- **优化器**：Adam(lr=0.001, β1=0.9, β2=0.999)
- **学习率调度**：余弦退火策略
  ```
  lr = lr_min + 0.5 * (lr_max - lr_min) * (1 + cos(epoch / total_epochs * π))
  ```
- **梯度裁剪**：最大范数5.0

### 6.3 训练流程
对每个批次的训练流程：

1. **前向传播**：
   - 编码输入序列和社交图
   - 根据调度采样率决定使用真实标签或模型预测
   - 对模型预测应用社交网络修正
   - 生成三个位置的预测

2. **损失计算**：
   - 计算位置加权预测损失
   - 计算社交一致性损失
   - 计算误差恢复损失
   - 组合得到总损失

3. **反向传播**：
   - 计算梯度并更新模型参数
   - 应用梯度裁剪和学习率调度

### 6.4 早停策略
- **验证指标**：验证集上的Hits@10
- **耐心值**：10个epoch
- **检查点保存**：保存最佳模型

## 7. 推理策略

### 7.1 集成解码策略
结合以下解码方法减轻误差累积：

- **束搜索解码**：
  - 维护k=5个最可能的序列候选
  - 综合考虑序列联合概率
  - 最终选择得分最高的序列

- **社交网络修正**：
  ```
  p_final = (1-α_t) * p_model + α_t * p_graph
  ```
  α_t随位置增加：第一步0.2，第二步0.3，第三步0.4

- **温度采样**：
  使用温度参数τ调整概率分布:
  ```
  p_i = exp(logit_i/τ) / Σ exp(logit_j/τ)
  ```
  τ=0.8，略微增加多样性

### 7.2 推理步骤
1. 编码输入序列和社交图
2. 第一步预测：生成k个候选节点
3. 第二步预测：对每个候选生成k个下一步候选
4. 第三步预测：对每个路径生成最终节点
5. 基于总得分选择最佳序列

## 8. 评估指标

根据要求，使用以下评估指标： 只对最后生成的3个传播序列节点进行指标核对：使用以下的指标

- **Hits@K** (K=10, 50, 100)：预测节点是否在真实前K个中的比例
- **MAP@K** (K=10, 50, 100)：考虑排序质量的平均精度

## 9. 实现注意事项

### 9.1 计算优化
- **稀疏矩阵**：社交图使用稀疏表示节省内存
- **批处理计算**：并行处理多个序列
- **GPU加速**：充分利用GPU计算能力

### 9.2 模型鲁棒性
- **Dropout**：在GRU层使用Dropout(0.2)
- **L2正则化**：权重衰减系数1e-5
- **数据增强**：通过不同长度切片增强训练数据

### 9.3 超参数选择
- **嵌入维度**：128-256
- **隐藏层大小**：256-512
- **GRU层数**：2-3层
- **学习率**：0.001，余弦衰减
- **批次大小**：32-64

## 10. GRU选择原因与优势

为本任务选择GRU而非Transformer的理由：

1. **参数效率**：GRU参数量少，训练更高效，适合中等规模数据集
2. **训练稳定性**：相比Transformer更容易训练，收敛更稳定
3. **序列长度适应性**：传播序列通常不会很长，GRU已足够捕捉必要的序列依赖
4. **自回归特性**：GRU天然适合自回归生成任务
5. **计算复杂度**：GRU的O(n)复杂度优于Transformer的O(n²)，尤其在推理阶段
6. **与图结构融合**：GRU的状态表示与图神经网络的节点表示容易融合

## 11. 总结

本设计方案通过社交图增强的GRU自回归模型，结合调度采样与曝光偏差训练策略，有效解决传播预测中的自回归误差累积问题。方案关键创新点包括：

1. **加权社交图构建**：从传播序列推断社交关系强度
2. **GRU与图注意力网络融合**：结合序列和结构信息
3. **混合训练策略**：调度采样+曝光偏差训练
4. **位置感知的损失设计**：针对不同预测位置优化
5. **集成解码策略**：束搜索+社交网络修正

该方案充分利用了GRU在自回归预测中的优势，并通过社交网络增强和专门的训练策略解决了误差累积问题，为高精度传播预测提供了完整解决方案。